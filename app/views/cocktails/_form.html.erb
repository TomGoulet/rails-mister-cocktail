<% content_for :title_tag, 'New Cocktail - Cocktails' %>

<%= simple_form_for(cocktail) do |f| %>
  <%= f.input :name %>
  <%= f.button :submit, 'Create', class: 'btn btn-primary' %>
  <%= link_to 'Cancel', cocktails_path, class: 'btn btn-default' %>
<% end %>
